on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest


    steps: 
      - name: 'Before'
        run: ls

      - uses: actions/checkout@v3
      
      - name: 'After'
        run: ls

      -name: "Login to Docker hub"
        run: docker login --username hagerelgyoshi --password dckr_pat_v3JFhefu0uCdORw_iPoup4gC6kE